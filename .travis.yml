language: elixir
elixir:
  - 1.2.2

addons:
  apt:
    packages:
    - nodejs

before_script:
  - phantomjs -w &

script: npm install && mix test && cd example && npm install && WEBDRIVER=phantomjs MIX_ENV=test mix compile && mix test
